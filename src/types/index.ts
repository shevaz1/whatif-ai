export interface TemplateMenuItem {
  id: string;
  title: string;
  description: string;
  path: string;
}

export interface TemplateSettings {
  pushEnabled: boolean;
  marketingConsent: boolean;
}

